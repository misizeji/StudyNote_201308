1：地址不能给地址赋值，需要用到strncpy；
2：发送命令时组包一定要%ld，最好是用宏；
3：拆包后要用GET_MODE(command),就会很简单；
4：组包格式一定是十六进制，发送文件时；
5：读取之后要对读取之后的数组进行清空；
6：取字符用%[^:]:之后取数字就直接格式化取；
8：显示百分比需要双浮点数进行除法运算；




BUG解决：

	解决发送给空地址问题；
	解决信号函数不能传参数；将sockfd定义成全局；